"""ESR v0.1 — Python SDK and CLI.

The public surface is re-exported here so users write `from esr import
handler, adapter, command, Emit, ...` without having to know the
submodule layout.
"""

__version__ = "0.1.0"
