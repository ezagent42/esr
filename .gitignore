# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-*/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# Elixir
/runtime/_build/
/runtime/deps/
/runtime/cover/
/runtime/doc/
/runtime/.fetch
erl_crash.dump
*.ez

# Local overrides / secrets
esr-cc.local.sh
.env
.env.local
.mcp.env
.esr/
~/.esrd/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Tool state
.claude/.ralph-loop.local.md
.claude/ralph-loop.local.md
.claude/scheduled_tasks.lock

# cc_mcp uv.lock is a side-effect of the CC-MCP-1 workaround
# (out-of-tree `uv run --with ...`). Do not track until cc_mcp
# is properly added to py/pyproject.toml uv.sources.
adapters/cc_mcp/uv.lock
