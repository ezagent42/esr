# ESR — durable TODO list

**Purpose**: persist work-in-flight + future-work items across CC sessions. Created 2026-04-29 because in-memory `TaskCreate`/`TaskList` is session-scoped and items got dropped between sessions.

**Owner**: this file is updated as part of any PR that:
- Defers a piece of work it could have done (mark as future)
- Discovers a new known issue (record + link)
- Closes a previously-tracked item (move to "Done — recent" or remove)

**Conventions**:
- One-line items with `[file:line](path)` or `[Spec §]` references when useful.
- "Status" column: `pending` / `in-flight` / `blocked` / `deferred`.
- `In flight` items name the PR / branch they live on.

---

## In flight (current PR)

(none)

## Pending — 2026-05-12 chat-flow validation findings

Manual test session 2026-05-12 (Feishu chat oc_d9b47511b085e9d5b66c4595b3ef9bb9) after `fix/register-adapter-spawns-faa` (PR #347) landed. Each row is a distinct issue surfaced during the wipe → boot → register_adapter → /feishu:bind → /session:new walk. To be batched into one or more follow-up PRs ("等会一起修复" — user directive).

| Tag | What | Notes |
|---|---|---|
| `chat-cap-check-username-to-uuid-hop` | `Esr.Resource.Capability.has?/2` (capability.ex:32-45) stops one hop short on the chat-side path | Today's chain: (1) direct lookup `ou_xxx` → miss; (2) `lookup_by_feishu_id(ou_xxx)` → username; (3) retry `Grants.has?(username, perm)` → miss when caps.yaml is UUID-keyed. **Add 4th hop**: `Esr.Entity.User.NameIndex.id_for_name(username)` → UUID → `Grants.has?(uuid, perm)`. **Why UUID-anchored, not username-keyed migration** (user-set memory rule 2026-05-12): UUID is the canonical identifier; username is mutable display-only. Caps stay UUID-keyed in storage; the translation chain handles ou_xxx → username → UUID at lookup time. Estimated ~30 LOC + 2 tests (chat-side cap-check hits the UUID; rename-username keeps caps working). Surfaced 2026-05-12 when `/session:new` returned `missing_capabilities` after `linyilun` (UUID `cba75063-...`) was bound — operator unblocked via `esr exec cap_grant target_principal_id=linyilun permission='*'` which writes a username-keyed row (works for now but is the wrong direction). Closes the "username-keyed unblock row" as a stop-gap. Related: [[feedback_uuid_is_canonical_identifier]]. |
| `session-start-failed-no-detail-visible` | `/session:new name=test-cc` returned generic `error: session_start_failed` without surfacing the `details:` field | `Esr.Commands.Session.New.spawn_session/9` (new.ex:702) wraps `{:error, reason}` as `Render.error(..., :session_start_failed, %{details: inspect(reason)})`. The chat-side renderer prints the `message` template (`"session start failed: %{details}"`) but operators saw only `error: session_start_failed` — the `details` interpolation looks dropped on the chat path. **Two issues bundled**: (1) what is the actual reason for THIS specific failure (likely workspace + agent_def resolution after wipe — `default_workspace_id` in user.json points to a workspace that no longer exists post-wipe), (2) chat-side render needs to actually substitute `%{details}` (or change envelope shape so the renderer always picks it up). Investigation path: capture next attempt's `details` via `esr exec session_new name=test-cc` (CLI path returns the raw map). Estimated: 30 min RCA + ~50 LOC fix + 1 regression test. Found 2026-05-12. |
| `sidecar-orphan-on-esrd-restart` | Python sidecar from previous esrd instance survives esrd restart with dead WS connection; new esrd never re-spawns and ETS routing finds nothing | Diagnosed 2026-05-12 live test. Sidecar (pid e.g. 65783) outlived parent esrd; new esrd boot's `WorkerSupervisor.ensure_adapter` saw "already running" (PID-level check passed) and skipped re-spawn, but the sidecar's WS was wired to the old esrd. Operator manually `kill 65783`'d → esrd auto-respawned (pid 4043) → healthy. **Fix path**: (a) at esrd boot, scan for orphan sidecars by pidfile/marker file and SIGTERM them before WorkerSupervisor's restore; (b) make WorkerSupervisor's "already running" check liveness-aware (probe sidecar WS health, not just OS PID). Option (a) is simpler. ~80 LOC + 1 integration test. The 2026-05-11 [`sidecar leak prevention`](../notes/) at_exit hook covers clean-shutdown leakage; this row is for crash/SIGKILL/launchctl-restart leakage. |
| `session-new-no-workspace-target-test-flaky` | `Esr.Commands.Session.NewNoWorkspaceTargetTest` fails when run as part of `mix test test/esr/commands/` but passes in isolation | Surfaced during PR #347's full-command-suite check (subagent report). Failure mode: `feishu-cc` template not loaded — bundle-loader test-ordering state. Test isolation bug not introduced by #347 (verified on clean `origin/dev`). **Fix**: setup block should `ensure_started` the bundle registry + register the feishu-cc template (mirror the pattern in `test/esr/integration/new_session_smoke_test.exs:67-89`). Estimated 20 LOC. |
| `register-adapter-half-state-surface-cleanly` | If `spawn_adapter` succeeds but `run_startup_hooks` fails inside `register_adapter`, the config + sidecar persist while FAA is missing — operator gets `register_adapter_failed` with no actionable guidance | By design per let-it-crash + the in-PR code comment (PR #347 register_adapter.ex:78-82), but operators can't tell whether the sidecar half ran. **Fix**: include in error envelope a `partial: ["sidecar_ok", "faa_failed"]` field listing which sub-steps completed, so operators know whether to re-run `register_adapter` (idempotent, will collide on sidecar) or `adapter_refresh` (FAA-only). ~30 LOC + 1 test. Tracked here so the half-state contract is testable + documented. |
| `audit-supervision-detect-strategy-flip` | `mix esr.audit_supervision` strategy detection blind spot (already in todo.md but reinforced 2026-05-12) | Currently audit drift gate would NOT catch a regression that flipped `:one_for_all` → `:one_for_one` on `AgentInstanceSupervisor` (silently breaking ADR-0002 CC+PTY pair invariant). Already in todo.md as `mix esr.audit_supervision strategy detection blind spot`; surface here because the register_adapter chat-flow walk depends on it (cap-check + supervisor strategy are the two structural invariants the chat path leans on). No additional work — link is enough. |
| `resolve-submitter-format-agnostic` | `Esr.Commands.Workspace.Resolve.resolve_submitter/1` (resolve.ex:71-79) only recognizes `submitter_username=X` and `submitted_by=ou_xxx` (Feishu open_id) — UUID submitted_by misses | Diagnosed 2026-05-12: CLI `esr exec session_new name=test-cc submitted_by=cba75063-...` (UUID) returned `no_workspace_target` because `resolve_submitter` tried `UserRegistry.lookup_by_feishu_id("cba75063-...")` (feishu_id ETS) → `:not_found`. Should add a third clause: if value matches UUID shape, look up via `Esr.Entity.User.NameIndex` (UUID → username). Anchors on UUID-canonical principle ([[feedback_uuid_is_canonical_identifier]]). Estimated ~25 LOC + 1 test (CLI submit with UUID resolves the user-default workspace). Operator-facing surface area: any CLI command that takes `submitted_by=<uuid>` (the natural form for ops-side authentication). |
| `merge-chat-context-missing-workspace-bind-chat` | `Esr.Entity.SlashHandler.merge_chat_context/3` (slash_handler.ex:742-824) has explicit clauses for session_new / session_end / session_list / workspace_info / workspace_new / pty_key only — `/workspace:bind-chat` and likely other chat-scoped commands hit the default pass-through clause and never get chat_id/app_id auto-injected | Diagnosed 2026-05-12: user in chat ran `/workspace:bind-chat name=linyilun-default` → got `missing required arg chat_id=<…>`. The command's `command_meta()` declares `chat_id, required: true` + `app_id, required: false (envelope auto-injects)` but the merge_chat_context default clause returns args unchanged. **Two fix options**: (a) add per-command clauses for every chat-scoped slash (mechanical; ~10 commands × 5 LOC); (b) make the default clause inject chat_id + app_id unconditionally and let per-command code reject if not allowed (~10 LOC total). Option (b) is more DRY but needs a per-command opt-out for CLI-only verbs. Audit needed: enumerate every command with `requires_user_binding: true` or chat-scoped args (chat_id, thread_id) to confirm coverage. Estimated 1-2 hours for full sweep + tests. |

## Pending — Plugin work multi-Phase plan (2026-05-05 brainstorm)

PR-180 series (today) = **Phase 1**: Loader + Manifest + 3 stub manifests + integration tests + RCA helpers + scenario-07 grep fix + in-process auto-confirm + tools/esr-debug.

| Phase | Spec | What | Notes |
|---|---|---|---|
| **Phase 1** — plugin loader foundation | `docs/superpowers/specs/2026-05-04-core-decoupling-design.md` (Spec A) + `2026-05-04-plugin-manifest-design.md` (Spec B) | Loader + Manifest + stub manifests + Loader.run_startup/0 + StatefulRegistry runtime API. | ✅ done — PRs #180–#187, #213, #219. |
| **Phase 2** — slash/CLI/REPL/admin unification | `docs/superpowers/specs/2026-05-05-slash-cli-repl-elixir-native.md` | Single dispatch path: delete Admin.Dispatcher, rewrite Python CLI as Elixir-native escript (`runtime/esr`), schema-driven CLI/autocomplete, voice plugin deletion. | ✅ **mostly shipped** — PRs #193 (voice deletion), #194–#202 (escript skeleton + ReplyTarget + Slash.* extraction + Esr.Admin.Dispatcher deletion), #211–#216 (slash routes for actors/cap/users), #222 (cli-channel→slash), #223 (Python CLI deletion). Net delete ~10–12k LOC vs original "~2500" estimate. **REPL still pending** (deferred to a follow-up — `esr exec /<slash>` is the current interactive surface). |
| **Phase 3** — plugin physical migration | `docs/superpowers/specs/2026-05-05-plugin-physical-migration.md` | Move feishu + claude_code modules from core into `runtime/lib/esr/plugins/<name>/`. Decouple cc agent_def from feishu. | ✅ shipped — PRs #203 (drop fallback Sidecar), #204 (StatefulRegistry), #205 (feishu move), #206 (cc move), #207 (cc no longer mentions "feishu"), #209 (drop permissions_registry.json), #219 (PR-3.4 feishu startup hook), #220 (PR-3.5 cc_mcp HTTP transport). |
| **Phase 4** — cleanup | (no formal spec — covered by per-PR field notes) | Stub manifest deletion (kept until Phase 3 done — they're now real plugins), legacy bash + websocat helpers, `Esr.Admin.*` namespace, `permissions_registry.json` JSON dump. | ✅ **mostly shipped** — PR-4.3 collapsed `Esr.Admin.{Supervisor,CommandQueue.*}` → `Esr.Slash.*` (#208); PR-4.4 dropped permissions_registry.json (#209); PR-22 deleted TmuxProcess + cc_tmux adapter (#163, #157). Stub manifests are now real plugin modules — no further deletion needed. |

## Pending — e2e harness gaps (post-PR-248)

| Tag | What | Notes |
|---|---|---|
| `periodic-e2e-replay` | Scheduled full-replay sweep against fenced guides | Phase 1 anti-drift gate is report-based (developer runs `scripts/generate-e2e-report.sh` locally, CI checks the file). That's trust-based. Mitigate by adding a `.github/workflows/e2e-periodic.yml` weekly cron job on `macos-latest` that runs `scripts/generate-e2e-report.sh` against `dev`, diffs against the latest committed report for that sha, and posts regression alerts to Feishu chat `oc_d9b47511b085e9d5b66c4595b3ef9bb9`. Spec authority: `docs/superpowers/specs/2026-05-10-guide-driven-e2e.md` §3.7 mitigation list. |
| `structured-reply-envelope` | Unify chat reply envelope (success + failure + warning) | Currently: success returns are Elixir-native `inspect/1` output (e.g. `ok: %{"action" => ..., "chat_id" => ...}`); errors are bare strings (e.g. `error: invalid_args`). Operators see inconsistent shapes between successes and failures. Needs a single schema generated by `SlashHandler` + `ReplyTarget.format_result/1`, with stable field names (`ok`, `error`, `warnings`, `data`). Related to existing in-memory task #220 "Spec: structured error/notification response system". Found by user manual test 2026-05-11 after `/workspace:use test-dev` returned the raw inspect map. |
| ~~`unconsumed-message-errors-not-hangs`~~ | ✅ **PARTIALLY CLOSED 2026-05-12** (adapter-spawn-gap branch) | The wipe→boot→register_adapter→silent-drop flavor of this bug is now closed by `fix/register-adapter-spawns-faa`: register_adapter atomically spawns both the sidecar and the FAA, so post-boot-registered adapters route inbound messages instead of dropping them with `no FeishuAppAdapter`. The "session with no agent bound" flavor is closed by the default-agent-and-agent-driven-flow plan (PRs #341-#344) which auto-binds a default agent on session creation. Both flavors of the original 2026-05-11 manual-test silent-drop are now covered. |
| `phase-3-fence-cc-reply` | Deferred Phase-2 fence pair: plain-text → CC reply (UNBLOCKED 2026-05-12) | Upstream gaps closed by default-agent-and-agent-driven-flow plan + `fix/register-adapter-spawns-faa`. Remaining work: add fence #5 (plain text → CC reply wildcard) to `docs/guides/flow-bootstrap.md` and rerun replay. ~10 LOC. |
| `scenario-19-m5-invariants-coverage` | Restore e2e-tier coverage of the 6 M-5/D4 invariants dropped when scenario 19 became a thin-wrapper | Pre-Phase-2 `19_session_first_default.sh` asserted 6 invariants from the session-first-default M-5 design: (a) `/user:add` auto-creates `<username>-default` workspace; (b) `/session:new` resolves to user-default; (c) `/workspace:add-folder` without `name=` routes via user-default; (d) `/user:use` switches the user-default; (e) re-`/session:new` post-`/user:use` binds the new default; (f) literal `"default"` workspace must NOT exist. Phase 2 thin-wrappered scenario 19 to test the bare-`/session:new name=` regression, dropping (a)–(f). Unit-level coverage in `runtime/test/esr/commands/session/new_resolution_test.exs:225` is partial. Decision: add `tests/e2e/scenarios/31_m5_invariants.sh` to restore e2e-tier assertions, OR accept that unit coverage is sufficient and document that decision. Spec reviewer C2 finding. |
| `ubuntu-ci-e2e-fixture` | Make ubuntu-latest GH Actions runner able to spawn the FAA Python sidecar | Today: only macOS can boot the e2e harness (FAA sidecar attach fails on Ubuntu with "no /ws client after 30s"). Phase 1 of guide-driven e2e routed around this with a report-based gate; fixing Ubuntu would let the CI gate run replay directly. Unblocks: actual CI-side anti-drift instead of trust-based, plus clears the existing `mix test on Linux CI fails ~22 of 1436 tests` TODO. |
| `e2e-14-routing` | Scenario 14: @mention routing via admin submit | session_new (admin-queue) creates a "pending" session with no Feishu binding. MentionParser + resolve_routing/2 only fires on real inbound Feishu messages. Needs mock_feishu → sidecar → runtime inbound path for session_new_surface-created sessions, OR a test-mode admin verb that injects raw inbound routing. |
| ~~`user-name-index-population` — User.NameIndex never populated~~ | ✅ **CLOSED 2026-05-07** by PR #250 | `Esr.Entity.User.Registry.populate_name_index/2` (line 241) calls `Esr.Entity.User.NameIndex.put/3`; wired from boot `:load_with_uuids` (line 192), `/user:add`, and `/user:remove`. Reverified 2026-05-09. |
| `e2e-15-principal-isolation` | Scenario 15: true cross-user cap rejection | All admin-queue commands run with submitted_by=ou_admin (wildcard caps). True "bob denied without grant" test needs a per-invocation principal switch (e.g. `ESR_OPERATOR_PRINCIPAL_ID=bob_15 esr_cli ...`) or an unprivileged fixture principal in capabilities.yaml. |

## Pending — concrete next PRs

| What | Tracked PR | Notes |
|---|---|---|
| ~~FCP non-text inbound: blocking receive → async pending_media~~ | ✅ **CLOSED 2026-05-09** by PR #311 (commit `80e55a0`) | `build_directive_fn/1` deleted. `Inbound.handle/2` now returns `{:directive, action, args, ctx} \| {:error, :unsupported_kind}`; FCP owns the async glue via `state.pending_media :: %{id => ctx}`, `handle_info({:directive_ack, ...})` resumes the post-download work, `handle_info({:pending_media_timeout, id})` (30s) cleans up. Regression test: text reply is not blocked while a download is in flight. |
| ~~Streaming SHA-256 in `Esr.Resource.Media.compute_sha256/1`~~ | ✅ **CLOSED 2026-05-09** by PR #311 (commit `c8963df`) | `compute_sha256/1` now streams via `File.stream!(path, 64 KB)` + `:crypto.hash_init/update/final` instead of `File.read/1`. Constant memory regardless of file size. Tests: known digest, 8 MB streamed vs reference, missing-file returns error. |
| Investigate: why E2E missed multi-adapter orphan duplication | pending | See task #222 (in-memory tracker). Audit final_gate.sh + tests/e2e/ for assertions that would catch "1 user message → N replies" — likely none. May get folded into the lifecycle migration PR's testing chapter. |
| ~~Spec: structured error/notification response system~~ | ✅ **CLOSED 2026-05-09** by the unified-command-grammar migration | Originally task #220. Folded into the 2026-05-09 unified-command-grammar spec as Pillar 3 (`Render.error/3`) and Pillar 4 (auto-generated `docs/grammar/errors.md`). All 77/78 command modules now use `Render.error(__MODULE__.command_meta(), :code, %{detail})` returning `{:error, %{"type" => ..., "message" => ...}}`. CI gate (`mix esr.check_command_docs`) prevents drift. |
| ~~Channel abstraction (per-session BEAM-supervised peer)~~ | ✅ **CLOSED 2026-05-10** by `2026-05-10-session-template-and-channel.md` (PRs #324–#330 + Phase 8 closeout) | Spawned during 2026-05-01 cc_mcp brainstorm + addressed *partially* by PR-3.5 (cc_mcp HTTP transport). The full abstraction now lives in `Esr.Channel` (behaviour + Registry), with `Esr.Plugins.ClaudeCode.Channels.McpHttp` + `Esr.Plugins.Feishu.Channels.ChatProxy` + `Esr.Plugins.StubAgent.Channels.NoOp` (test-only) as concrete impls. Plugins declare channel kinds in their `manifest.yaml` `channels:` block; SessionTemplate composes them per-session. Open issue: `docs/issues/02-cc-mcp-decouple-from-claude.md` — runtime portion subsumed; doc-side closing note tracked in the spec's §11 references. |
| `esr scenario run` port + Python SDK trim | deferred from PR-223 | Python click CLI is gone (PR-223), but `py/src/esr/` still has the SDK (`adapter.py`, `handler.py`, `events.py`, `actions.py`, `ipc/handler_worker.py`) used by adapters/feishu/ + handler sidecars. The yaml-driven `esr scenario run` was deleted with the CLI — its functional replacement is `tests/e2e/scenarios/0X_*.sh` + `make e2e-N`. No port needed. SDK stays as long as Python adapter sidecars exist. |
| `esr daemon init` + `esr daemon clear` + esrd home redesign | candidate next PR (unblocked: workspace + metamodel-aligned redesigns shipped 2026-05-07; ESRD_HOMEs already wiped) | Two related escript subcommands:<br>• `esr daemon clear [--instance=<n>] [--keep-data] [--force]` — fold `tools/wipe-esrd-home.sh` into the escript with proper instance scoping + dry-run. Standalone shell script remains as fallback during the transition.<br>• `esr daemon init [--force] [--admin=<user>] [--plugins=<list>]` — one-command Bootstrap: seeds default workspace + admin user + enabled plugin list from CLI params; binary-managed yaml (slash-routes, agents) re-copies from priv at every boot (prevents the schema-drift bug that broke `/help` after PR-198 renamed `Esr.Admin.Commands.*` → `Esr.Commands.*`).<br>Estimated ~250 LOC for clear + ~700-1000 LOC for init. The metamodel-aligned redesign (spec 2026-05-07) provides the storage layout (sessions/<uuid>/ + users/<user_uuid>/ + workspaces/<name>/) so `init` knows what to create. |
| Hot-reload of plugin config (Phase 2 of plugin-config) | candidate next PR (decided 2026-05-07: explicit `/plugin:reload [plugin]` slash + manifest `hot_reloadable: true` opt-in + `Esr.Plugin.Behaviour.on_config_change/2` callback) | Today plugin config is restart-required. Spec rev-3 adds: explicit slash trigger (no fs-watcher); per-plugin opt-in via manifest; runtime callback for plugins to react. Estimated ~400 LOC + tests. |
| ~~pty_attach_security_hardening~~ | ✅ **CLOSED 2026-05-09** by PR #314 | `EsrWeb.PtySocket.connect/1` now requires a `Phoenix.Token` signed token (salt `pty_attach`, 10-min TTL); `?sid=` query auth removed. The attach URL no longer carries actor_id at all (`/sessions/attach?token=<sig>`); HTML page reads `ESR_TOKEN` for the WebSocket connect, `ESR_SID` only for display. AttachController returns 403 on verify failure. 7 token-verification tests cover valid / tampered / wrong-salt / expired / missing / empty / round-trip. |
| ~~`/key agent=<name>` UX shorthand~~ | ❌ **DROPPED 2026-05-09** | Original idea: `/key agent=alice` as friendly chat-current primary-agent toggle, sugar over `/agent:set-primary`. Killed because the name collides with `/pty:key` (keyboard input → PTY) — overloading the same prefix for two unrelated concepts is bad UX. `/agent:set-primary` already covers the function; if a shorter alias is wanted, pick one that doesn't collide (e.g. `/agent:focus`) and decide separately. |
| Plugin overlay loader integration test for `/feishu:bind` + `/claude_code:tui` (M-1 carry-over from resource-typed grammar Phase E) | deferred | Phase E shipped `/claude_code:tui` via the claude_code plugin manifest's `slash_routes:` block (the second consumer of plugin-scoped command registration after feishu's overlay). Unit + e2e coverage is in place for both. The integration test that proves *both* plugins load their overlay from disk (not just the in-process manifest registration) is still missing — would catch a regression where `Esr.Plugin.Loader` silently drops one plugin's `slash_routes:` block. |
| Operator-migration note: `~/.esrd` local stale state after grammar cutover | deferred docs | Operators upgrading from pre-rev-3 grammar may have stale shell history / muscle memory pointing at `/session:add-agent` etc. The `slash_handler` deprecation hints fire at runtime, but a one-page migration note (alias suggestions, common typos, where the new verbs live) would smooth the transition. M-2 carry-over from resource-typed grammar Phase D. |
| **CI `mix test` on Linux** | deferred 2026-05-09 from PR #305 (Phase 5 ci.yml) | The new ci.yml builds + runs `mix esr.check_command_docs` (the grammar drift gate). `mix test` is intentionally **not** wired in yet — running the suite on Ubuntu surfaced ~22/1436 failures unrelated to the unified-grammar migration: (a) `claude` CLI not on PATH (PTY spawn in scenario tests), (b) test isolation bugs visible only with Linux test ordering (real but pre-existing), (c) YAML fixture parse errors in some cap tests. Suite passes on macOS dev. Followup: triage each class, tag the not-Linux-portable ones, then uncomment the `Run tests` step in `.github/workflows/ci.yml`. The TODO comment block in that file enumerates the known-failing classes. |
| ~~Chat-envelope arg auto-injection gaps (yellow-fix-cluster A)~~ | ✅ **CLOSED 2026-05-09** by PR #293 | `/agent:add type=cc name=test-cc` now resolves `session_id` from chat-current via `Esr.Resource.Session.ChatRouting.Registry.current_session/2`; `/session:new name=...` returns structured `no_workspace_target` (not generic `invalid_args`) when no resolver layer matches. **Long-term parking lot:** when the Realm spec lands (Realm = define business flows like feishu→CC), fold the resolver layer into the Realm's per-flow arg-injection table — each Realm declares which envelope fields auto-inject for which command class. Today's fix is per-module duplication; Realm will dedupe. |
| ~~Admin-CLI submit path generates non-UUID `session_id` (base32 fallback)~~ | ✅ **CLOSED 2026-05-09** by PR #280 (UUID swap) + PR #317 (mint unification) | The base32 → UUID swap actually landed in PR #280 (rev-3 grammar Phase F docs sweep also wrote this todo entry, with stale line numbers from before the in-PR fix). PR #317 unified all three remaining `UUID.uuid4()` call sites (admin-CLI submit, chat-bound mint, agent_spawner spawn fallback) into `Esr.Resource.Session.Id.new/0` so the Phase 5 D2 contract (session ids must be UUID v4 for cap routing) lives in one place. Strategy changes (UUID v7 / deterministic test mode) now touch one file. |
| feishu_bind / feishu_unbind don't update users/<uuid>/user.json (#290 + #288) | inherited by /feishu:bind in `2026-05-09-principal-id-semantic-split.md` | `Esr.Plugins.Feishu.Commands.BindUser.execute/1` and `UnbindUser.execute/1` only write `users.yaml`, not the per-uuid `users/<uuid>/user.json`. After bind/unbind the two files diverge on the `feishu_ids` field. **The new `/feishu:bind` slash (SelfBind → BindUser delegate) inherits this bug** — same yaml↔json drift. Today's `Esr.Entity.User.FileLoader.load/1` is yaml-canonical so runtime is correct, but: (1) backup/restore via `load_from_users_dir/1` would silently drop bindings; (2) future migration making `user.json` canonical hits stale arrays; (3) operators inspecting json see authoritative-looking stale data. **Two fix options** — needs separate brainstorm: (a) atomic write to BOTH files in BindUser/UnbindUser; (b) remove `feishu_ids` from `user.json` entirely, leave it carrying only static identity. Family with #288 ("C5 — user.watcher + bind reload"). Should be fixed together. Out of scope for the principal-id-rename PR. References: `runtime/lib/esr/plugins/feishu/commands/{bind_user,unbind_user}.ex`, `runtime/lib/esr/entity/user/file_loader.ex`, `docs/notes/2026-05-09-cold-start-walkthrough-1.md` C10. |
| `username` semantic split (sibling of `principal_id` rename) | observed 2026-05-09 during /feishu:bind subagent review | `Esr.Entity.SlashHandler.merge_chat_context/3` (`runtime/lib/esr/entity/slash_handler.ex:705,723`) uses `maybe_put("username", ...)` for `session_new` / `session_end` (and similar). Same vulnerability shape as the `principal_id` conflation that spec `2026-05-09-principal-id-semantic-split.md` resolves: a chat member could type `/session:new ... username=victim` and the envelope-resolved username would be discarded by `Map.put_new`. Different blast radius — caps key on `principal_id` not `username`, so a spoofed username may not directly grant unauthorized capabilities, but downstream session ownership / URI shaping (`esr://...sessions/<username>/...`) could end up wrong. **Approach**: same playbook as the `principal_id` split — audit consumers of `args["username"]`, classify each as caller (envelope-resolved) or target (user-supplied, e.g. `/cap:grant target_username=bob`?), introduce `caller_username` and (if needed) `target_username`, switch the `merge_chat_context` injectors to `force_put` for `caller_username`. Effort: ~50 LOC + per-clause audit + tests. |
| Pure-username caps migration (#238) — accelerator note | observed 2026-05-09 during /feishu:bind brainstorm | The legacy ou_xxx-keyed bootstrap-cap auto-grant path (referenced in `runtime/lib/esr/plugins/feishu/commands/bind_user.ex` moduledoc) is still active because not every Feishu user has an esr username yet. **The `/feishu:bind` self-service slash (spec `2026-05-09-principal-id-semantic-split.md`) directly accelerates #238**: once Feishu users can self-bind from inside a chat, the username-coverage rate climbs without operator intervention, shrinking the legacy ou_xxx-cap surface. After /feishu:bind ships and adoption settles, audit `capabilities.yaml` for residual ou_xxx-keyed entries and re-evaluate whether #238's full cutover can land. |
| Strict-args (`@allowed_keys` whitelist) project-wide adoption | observed 2026-05-09 during /feishu:bind brainstorm | The `/feishu:bind` + `/feishu:unbind` commands (spec `2026-05-09-principal-id-semantic-split.md` § 4.3 / § 4.4) introduce an `@allowed_keys` whitelist on `args` that rejects any non-declared key as `invalid_args`. The pattern improves UX (typo feedback) and security (closes identity-injection attacks). Currently only SelfBind / SelfUnbind use it; the other ~30 commands under `runtime/lib/esr/commands/` use permissive pattern-matching and silently ignore extras. **NOTE — depends on `use Esr.Commands.Meta`** (spec `2026-05-09-unified-command-grammar-and-errors.md`, committed 33c316b, in flight): once that spec lands, every command exposes a `command_meta/0` callback with a declared `args:` list, which becomes the natural single-source-of-truth for the allowed-key set. The right sequencing is: (1) wait for `Esr.Commands.Meta` to land; (2) add `reject_unknown_args/1` in the Meta-injected machinery (or `Esr.Role.Control` helper) that reads `command_meta().args` plus a fixed env-injected key allowlist (`principal_id`/`chat_id`/`app_id`/`thread_id`/`username`); (3) flip strict-args on per-command, removing the local `@allowed_keys` from SelfBind/SelfUnbind in the process. Risk: existing callers / automation may currently pass extras that work today and would break under strict mode — audit + deprecation path needed. |
| `mix esr.audit_supervision` strategy detection blind spot | observed 2026-05-11 during PR-3 code-quality review | `audit_supervision.ex:179-210` returns `(unknown)` for `Elixir.Registry` / `DynamicSupervisor` / standard `Supervisor` because the tuple-arity pattern only covers one shape. The audit drift gate detects supervisor add/remove but does NOT detect a strategy flip (e.g. `:one_for_all` → `:one_for_one` on `AgentInstanceSupervisor`, which would silently break ADR-0002's CC+PTY pair invariant). ADR-0002 line 70-73 acknowledges the gap. **Fix options**: (a) extend `strategy_of/1` to handle each supervisor type's state-tuple shape via per-module sniff; (b) add a separate `grep`-based CI gate over `strategy: :one_for_all`/`:one_for_one` declarations in known supervisor files. (a) is structurally cleaner; (b) is 10 LOC and immediately works. Estimated 30 min for (b), 2-3 hours for (a). |
| Integration-level I3 / I4 invariant tests | observed 2026-05-11 during PR-3 review | `test/esr/system/invariants_test.exs` I3 and I4 currently run as routing-level stubs (test process injected as FCP via direct ETS write to `:esr_actor_role_index`). The real path needs `Esr.Session.Router.create_session/1` with a writable workspace + a live FAA registered for `app_id`. **Fix**: write an `Esr.System.IntegrationInvariantsTest` (tag `:integration`) under `test/esr/integration/` that boots the full feishu-cc bundle in `setup_all` and re-asserts I3 (real session_dir write/delete on supervisor lifecycle) + I4 (real chat-visible reply via FAA's outbound channel). Reuses the stub fixture's structure but with the real spawn path. Estimated 100 LOC + fixture wiring. |
| `submit_slash` principal_id fallback | observed 2026-05-11 during PR-4 review | `feishu_chat_proxy.ex` `run_submit_slash/3` reads `state.principal_id`. FCP state defaults `principal_id ||= ""` when neither spawn args nor `proxy_ctx` carry it. The synthetic msg_received envelope then carries `"submitted_by" => ""`, and SlashHandler rejects with `missing_capabilities`. This **fails closed** (safe), but the user sees a confusing capability error rather than a clear "session has no operator binding" message. **Fix**: in `run_submit_slash/3`, when `state.principal_id` is `""`, fall back to `session.owner_user` (verify field exists in Session.Struct). If both are absent, return `{:error, %{"kind" => "no_operator"}}` with a clearer message. Estimated 10 LOC + 1 test. |
| `:real_claude` CI job — install claude binary | observed 2026-05-11 during PR-4 review | `.github/workflows/ci.yml` `real-claude-integration` job runs on macos-latest, which doesn't have `claude` preinstalled, so the test currently SKIPs vacuously (green but no coverage). **Fix**: add an "Install claude" step (probably `npm install -g @anthropic-ai/claude-code` or whatever the stable installer is at the time). Cache the install per-runner. Estimated 5 LOC ci.yml + verify install path. Until then the job is a placeholder for the install hook landing. |

## Pending — design discussions before PR

| What | Why deferred | Notes |
|---|---|---|
| Auto-bind feishu Option B/C (full automation with confirmation DM) | PR-21i shipped Option D (instructional DM only) | Original brainstorm in conversation 2026-04-29 ~02:00. User chose Option D for now. Revisit if instructional-DM friction is still too high after live use. |
| OAuth-based esr user registration | Spec §"Out of scope" | Manual `esr user add` for now. Real OAuth needs Feishu Open Platform integration. |
| **Yaml-ify adapter sidecar dispatch** (`@sidecar_dispatch`) | Workshop 2 | After Workshop 1 (slash routing) ships. `worker_supervisor.ex:45` map. Adding new adapter type today touches Elixir. |
| **Yaml-ify permissions registry** | Workshop 2 | Centralize the `permissions/0` callbacks scattered across `Esr.Admin`, `Esr.PeerServer`, future handlers. Single `permissions.yaml` listing perms + scope prefixes (would have prevented PR-21γ's `validate_scope` bug). Note: PR-4.4 dropped the file-system JSON dump but the in-memory registry remains scattered. |
| **Yaml-ify default values** (agent="cc", role="dev", start_cmd, kill_timeout) | Workshop 3 | `defaults.yaml`. Currently scattered across 5+ modules; tests + dev quirks suffer from no central spec. |
| **Yaml-ify rate-limit windows** (`@deny_dm_interval_ms`, `@guide_dm_interval_ms`) | Workshop 3 | Operator-tunable. Dev / prod / e2e want different values. |
| **Externalize doctor / help text to markdown** | Edge / cosmetic | Long Chinese heredoc strings in `Esr.Commands.Doctor`. Operator wants to tweak phrasing without restart. |
| **Per-workspace worktree path convention** | Speculative | Today PR-21θ hardcodes `<root>/.worktrees/<branch>`. Workspace yaml could carry `worktree_pattern:` to override (e.g. `<root>-<branch>` sibling-dir). |
| **Yaml file visualization / pretty-print** | New 2026-04-30 | After PR-21κ, the yaml surface grew: `slash-routes.yaml` + workspace JSON files (per-workspace, hybrid storage post-2026-05-06) + `users.yaml` + `capabilities.yaml` + `adapters.yaml` + `plugins.yaml` + `manifest.yaml` (per plugin) + `bundles/<name>/{manifest,template}.yaml` (post-2026-05-10 SessionTemplate migration) — cross-references hard to follow. **Note 2026-05-10:** `agents.yaml` dissolved by `2026-05-10-session-template-and-channel.md` Phase 6; agent kinds now live in plugin manifest's `agent_kinds:` block. `docs/grammar/templates.md` (Phase 8) is the auto-generated bundle reference; a unified topology view across all yaml types is still future work. Candidates: (a) `/topology` slash, (b) `runtime/esr show` pretty-printer, (c) mermaid/graphviz at boot, (d) Phoenix LiveView dashboard. Brainstorm + scope decision needed. |
| **Auto-create session: meaningful default cwd** | New 2026-05-01 | When a chat with no live session receives non-slash text, FAA broadcasts `:new_chat_thread` and Scope.Router auto-creates a session. PR-21τ added a safety net (`pwd` fallback). User suggestion: use `$ESRD_HOME/<env>/<workspace>/sessions/<sid>/` so ESR self-manages the path. Tradeoff: claude has no git context there. Brainstorm before implementing. |
| **Spec: agent (cc) startup config first-class** | New 2026-05-01 (carries forward post-2026-05-10 sessiontemplate migration) | Today CC's startup is a stack of moving parts: claude_code plugin's `agent_kinds.cc` entry (lives in plugin manifest as of 2026-05-10 Phase 6, replacing the legacy `agents.yaml`), workspace `start_cmd` (now in `workspace.json` post-2026-05-06), `Esr.Plugins.ClaudeCode.Launcher` (Phase 8: `scripts/esr-cc.sh` deleted), launchd plist EnvironmentVariables, claude's own `~/.claude/` cached creds. Operator-on-fresh-host hitting `Please run /login - 403` has no single doc. Candidates: (a) `agent_kinds.<kind>.preconditions:` block + startup validator, (b) `/doctor` agent-aware (Anthropic API reachability + cred check), (c) deploy doc enumerates the layers. Triggered by 2026-04-30 PR-21κ live-test where 4+ layers had to be debugged. |
| ~~Reliability: PtyProcess-death zombie session~~ | ✅ **CLOSED 2026-05-09** by PR #403 (M-2.6, shipped earlier) | `Esr.Session.AgentInstanceSupervisor` (`runtime/lib/esr/session/agent_instance_supervisor.ex:40`) runs CC + PTY under `strategy: :one_for_all, max_restarts: 3, max_seconds: 60`. PtyProcess crash kills + restarts CCProcess in lockstep; new pids re-register in InstanceRegistry; FCP resolves by name (not cached pid) so the next message lands on the new instance. No zombie window. (Audit reverified 2026-05-09.) |
| **BEAM lesson note: supervisor reentrancy in child init** | New 2026-05-01 | PR-21ψ deadlock taught: never call back into your own supervisor (or its children) from within a child's `init/1`. `DynamicSupervisor.start_child` is synchronous; cycling back into the supervisor's mailbox → infinite wait. Escape hatches: `Process.send_after`, `{:continue, :step}`, separate process. Worth a `docs/notes/beam-init-reentrancy.md` so this doesn't repeat. |
| **`/session:switch name=<n>` slash for explicit chat-current rebind** | New 2026-05-01 | The chat-current overwrite shipped in PR-21l is implicit: a second `/session:new` silently displaces the prior session. UX-wise an explicit `/session:switch` is preferable. Implementation: parked_sessions map per (chat, app) + current_sid pointer; `slash-routes.yaml` entry for `session_switch`; `Esr.Commands.Scope.Switch` (stub exists) resolves `name=<n>` via `claim_uri` index. Real design work (~150–200 LOC + tests + spec). |
| ~~**Feishu file / image / audio inbound**~~ | ~~New 2026-05-02~~ | ~~FAA + FCP only handle `msg_type: text`. Feishu's `im.message.receive_v1` also delivers `file` / `image` / `audio` / `media` / `sticker` / `post` / `interactive` payloads with `file_key` references that need to be downloaded via Lark API.~~ **Closed 2026-05-08** — image + file inbound shipped (spec `2026-05-08-multimedia-content-protocol-design.md`). Audio + other types tracked as deferred follow-ups below. |
| **Audio Phaser + Feishu audio inbound/outbound** | spec deferred 2026-05-08 | cc TUI doesn't natively consume audio bytes; audio surfaces as a path that claude can dispatch other tools (e.g. Whisper) against. Single Phaser + parser tweak per `docs/notes/multimedia-protocol.md` "Adding a new media type". |
| **Resource GC for `resources/<media_type>/<sha256>.*`** | spec deferred 2026-05-08 | `Esr.Resource.Media.RefIndex.gc_loop/0` periodic task: refs.jsonl empty + last_seen > 30d → safe to delete. Boot scan rebuilds `.meta.json` from `.refs.jsonl` on demand. |
| **Other Feishu media types: video / post / interactive cards / sticker / share_chat / share_user / location** | spec deferred 2026-05-08 | each is one Phaser + one parser tweak after the current spec lands |
| **Outbound image rendered as `msg_type=image` (not file)** | spec deferred 2026-05-08 (Phase 3 follow-up) | The α-wire `_send_file` directive in the Python sidecar always issues `im.v1.message.create` with `msg_type=file` regardless of the Elixir-side `media_type`. Bytes round-trip correctly (verified by scenario 21), but operators see the image as a file attachment in Feishu UI rather than an inline image preview. Fix: split the sidecar's outbound dispatch on `media_type` — image goes through `im/v1/files` upload + `msg_type=image` send; file via the existing path. Small Python sidecar change (~30 LOC) + scenario assertion update. |
| **`/session:new <name>` redesign — auto-cwd + add-dir + enter-worktree** | New 2026-05-02 | Today's `/session:new name=X dir=Y` packs parameters that overlap with workspace concerns. User-proposed refactor: `/session:new <NAME>` (auto-cwd at `$ESRD_HOME/<env>/users/<username>/sessions/<NAME>/`), `/session:add-dir <PATH>` (registers project dir + claude `--add-dir`), `/session:enter-worktree <BRANCH>` (creates worktree + chdirs). Brainstorm + spec before implementation. |
| **Pre-merge-dev gate: agent-browser content assertion (3 of 3-2-1)** | New 2026-05-04 — partial in PR #184 | Current gate (`scripts/hooks/pre-merge-dev-gate.sh` §2b) checks `data-opened-cols`/`-rows` dataset which populate at xterm.js init BEFORE WS data. PR #184 added partial content assertion (item 2 of 3-2-1). Item 1 (RCA) and item 3 (full content) still pending. Memory rule §K mandates agent-browser for web/UI work. |
| **R7.5: Esr.Commands.Scope.BranchEnd split** | New 2026-05-04 | BranchEnd at 453 LOC bundles 4 separable concerns: cleanup handshake, worktree script execution, branches.yaml mutation, routing.yaml mutation. Splittable into orchestrator (~50 LOC) + `Esr.Resource.Branches` + `Esr.Worktree.Cleanup`. Spec needed before implementation. |
| ~~First-user-auto-admin~~ | **CLOSED 2026-05-09** by PR `feat/auto-admin-on-first-user` — `Esr.Commands.User.Add.maybe_grant_admin/1` runs `Capability.Grants.any_admin?/0` after success and appends `["*"]` to capabilities.yaml when no prior admin exists. `ESR_BOOTSTRAP_PRINCIPAL_ID` env stays as complementary seed mechanism. Audit step 2 → ✅. |
| **Migrate to session-first model** | ✅ Phase 1 shipped 2026-05-08 — user-default replaces system default, /user:use, auto-create on /user:add. Phase 2 (`/session:add-folder` mutating running scope) deferred. | `docs/notes/concepts.md` metamodel IS session-first (Scope = runtime instance of a Session; workspace = Resource referenced by Scope). Current implementation is workspace-first (workspace must pre-exist; session spawns against it). PR #230 fixed workspace storage but slash surface is still workspace-first. Aligning impl with metamodel = let `/session:new` lazily create transient workspace; add `/session:add-folder`, `/session:bind-workspace`. Big refactor — needs brainstorm before commitment. Surfaced by audit Steps 9 + 10. |

## Pending — observability / ops

| What | Notes |
|---|---|
| `ChannelClient` align with phx-py reference | `docs/futures/channel-client-phx-py-alignment.md` — full audit of self-hosted Phoenix client vs phx-py best practices. PR-21l fixed heartbeat; per-call timeout + connection-state events still missing. |
| ExDoc-based runtime API docs | The previous `gen-docs.sh` script (covered click CLI + cli_channel.ex dispatch topics) was deleted in PR-223 along with the Python CLI. The live source-of-truth for slashes is now `runtime/priv/slash-routes.default.yaml` + `EsrWeb.SlashSchemaController`'s `/admin/slash_schema.json` endpoint that `runtime/esr describe-slashes` queries. Standalone ExDoc for internal Elixir modules is still missing. |
| Per-session role override | Workspace dictates `role:` today; per-session override mentioned in spec out-of-scope. |
| Cross-workspace branch sharing | Speculative; spec out-of-scope. |
| Worktree GC sweep | Periodic prune of branchless worktrees. Operator runs `git worktree prune` manually for now. |

## Done — recent (last ~15 PRs, for context)

Older PRs are in git log.

- feature/workspace-vs-code-redesign-impl — **workspace VS-Code-style redesign shipped after merge**: hybrid storage (`workspace.json` per workspace, ESR-bound or repo-bound), UUID identity, `folders[]` replacing single `root`, `Workspace.Watcher` deleted (CLI invalidates Registry inline), 1:N workspace-to-sessions, 14 new `/workspace*` slashes, `default` workspace auto-created at boot. Spec: `docs/superpowers/specs/2026-05-06-workspace-vs-code-redesign.md`.
- PR #178 — R11 post-mortem note (e2e lessons + agent-browser content gate TODO)
- PR #179 — plugin work specs (Core Decoupling A + Plugin Mechanism B)
- PR #180–#186 — Plugin Track 0 / Phase 1: Loader + Manifest + 3 stubs + RCA helpers + boot bridge
- PR #187 — `tools/esr-debug` reusable RCA helper bundle
- PR #188 — Phase 2/3/4 plugin work specs (中英双语) + initial todo.md update
- PR #190–#191 — channel-server port debt audit + delete daemon.py + dev_channels_unblock.sh
- PR #192 — Phase 2-3-4 execution plan + AFK operating principles
- PR #193 — delete voice plugin (never productionized) (Phase 2 PR-2.0)
- PR #194 — `GET /admin/slash_schema.json` schema dump (Phase 2 PR-2.1)
- PR #195–#197 — `Esr.Slash.ReplyTarget` behaviour + 4 impls (Phase 2 PR-2.2 / PR-2.3a / PR-2.3b)
- PR #198 — rename `Esr.Admin.Commands.*` → `Esr.Commands.*` (Phase 2 PR-2.4)
- PR #199 — delete `Esr.Admin.Dispatcher`; SlashHandler unifies dispatch (Phase 2 PR-2.3b-2)
- PR #200–#201 — `Esr.Cli.Main` escript skeleton + subcommands (Phase 2 PR-2.5/PR-2.6)
- PR #202 — close Phase 2 (dev-guide CLI install + completion note)
- PR #203 — remove fallback Sidecar registrations (Phase 3 PR-3.1)
- PR #204 — `Esr.Entity.Agent.StatefulRegistry` runtime API (Phase 3 PR-3.2)
- PR #205 — move feishu modules to `runtime/lib/esr/plugins/feishu/` (Phase 3 PR-3.3)
- PR #206 — move cc modules to `runtime/lib/esr/plugins/claude_code/` (Phase 3 PR-3.6)
- PR #207 — cc plugin no longer references "feishu" (Phase 3 PR-3.7)
- PR #208 — collapse `Esr.Admin.*` → `Esr.Slash.*` (Phase 4 PR-4.3)
- PR #209 — drop `permissions_registry.json` cross-language dump (Phase 4 PR-4.4)
- PR #210 — Phase 3 + Phase 4 closing status note
- PR #211 — e2e CLI dual-rail switch (Phase A)
- PR #212 — escript output format + `/actors` slash route (Phase B-1)
- PR #213 — delete StatefulRegistry hardcoded fallbacks (Phase D-1)
- PR #214 — escript click-style flag parser + `/cap {list,show,who-can,grant,revoke}` (Phase B-2)
- PR #215 — `/users {list,add,remove,bind-feishu,unbind-feishu}` slash routes (Phase B-3)
- PR #216 — delete `py/src/esr/cli/` (Phase C — reverted in #218)
- PR #217 — manifest validation gate test (Phase F / PR-4.5)
- PR #218 — revert Phase C (final_gate.sh was broken)
- PR #219 — feishu plugin startup hook + `Esr.Plugin.Loader.run_startup/0` (Phase 3 PR-3.4)
- PR #220 — cc_mcp HTTP MCP transport (esrd-hosted) (Phase 3 PR-3.5)
- PR #221 — PostToolUse self-check hook after openclaw-channel calls
- PR #222 — cli-channel→slash migration: every `cli:*` handler → `Esr.Commands.*` slash; cli_channel.ex shrinks to a 30-line protocol shell; new `Esr.Resource.Workspace.Describe` single security boundary
- PR #223 — scenarios/ + Python CLI removal: ~10k LOC deletion + 3 unit-test gaps closed (`Esr.Commands.{Deadletter,Debug,Trace}`)
- PRs #224–#244 — metamodel-aligned ESR redesign (Phases 1–8): colon-namespace slash surface, session-first entity model, `Esr.Entity.Agent.InstanceRegistry`, cap UUID-only enforcement, 3-layer plugin config, esr-cc.sh deletion + e2e migration — spec `docs/superpowers/specs/2026-05-07-metamodel-aligned-esr.md`. ✅ complete (Phase 9 = this PR, final docs + e2e 14/15/16).
- PRs #281–#284 — zero-config bootstrap (PR #281 spec + plan, #282 first user_add auto-admins via sentinel, #283 register_adapter passes app_secret to feishu Python sidecar, #284 docs/guides/{flow-bootstrap,feishu-adapter-setup}.md).
- PR #285 — fix(doctor): refresh hardcoded grammar to post-2026-05-09 forms (next_steps_text/5 was rendering `/new-workspace` + `esr --env=dev feishu bind`).
- **PRs #286 + #291** — unified-command-grammar **spec** + **implementation plan** (en + zh_cn); landed alongside the migration commits.
- **PRs #293–#306 (today, 2026-05-09)** — unified-command-grammar migration in 13 PRs: yellow-fix A (#293, /agent:add chat-current fallback + /session:new no_workspace_target), Phase 1 DSL infra (#294, Esr.Commands.Meta + Render + ResourceHelp + Ymlr deps), Phase 2 canary (#295, Workspace.New on DSL), Phase 3.2-3.11 (#296/#297/#299–#303, 76 modules to DSL), Render integer-interpolation fix (#298), Phase 4 derived yaml + docs (#304, mix esr.gen_slash_routes/gen_command_docs/check_command_docs + docs/grammar/{commands,errors}.md), Phase 5 CI (#305, .github/workflows/ci.yml + grammar drift gate), Phase 6 (#306, bare-prefix help routing). Plus #307 mix-tasks Application.load fix (CI compatibility). **77/78 command modules now declare grammar via `command_meta/0`; CI gate prevents drift; `/workspace` bare-prefix help works live.**
- PR #316 — pty_attach URI path: `/sessions/attach` → `/ptys/attach` (resource consistency: matches `/pty:attach` slash + `Esr.Uri` path-style type `ptys`).
- PR #317 — session_id mint unification: `Esr.Resource.Session.Id.new/0` is the single source for UUID v4 session ids; removes 3 duplicated `UUID.uuid4()` call sites + their per-site "Phase 5 D2" reminder comments. Closes the base32-fallback todo entry.
- PR (manual-checks-to-guides, today) — distill 12-step bootstrap audit into `docs/guides/operator-bootstrap-checklist.md` (en + zh_cn); manual-checks/README.md updated to point at new guide; dated audits stay as historical record. 12/12 steps confirmed closed.
- PR #314 — pty_attach security hardening: Phoenix.Token + 10-min TTL replaces `?sid=` query auth. URL no longer carries actor_id. 7 token-verification tests + drift gate green.
- PR #313 — todo.md closeouts: drop `/key agent=<name>` UX (bad name) + close PtyProcess-death zombie (M-2.6 supervisor handled).
- PR #311 — multimedia hardening: streaming sha256 + FCP non-text inbound async (deletes 30s blocking receive). Closes 2 todo.md "concrete next PRs" rows.
- PR #287 — cold-start friction fixes (jjkysy): C2 feishu manifest no longer mis-classifies app_id/app_secret as plugin-level config, C3 UnboundChatGuard onboarding text uses colon-namespace, C6 feishu Bootstrap logs Logger.info on absent adapters.yaml, C8 escript ESRD_HOME default aligns with runtime default.
- **PRs #324–#330 + Phase 8 (2026-05-10)** — SessionTemplate + Channel migration in 8 phases: spec `docs/superpowers/specs/2026-05-10-session-template-and-channel.md`. Phase 1 (Channel behaviour + Registry), Phase 2 (claude_code MCP HTTP Channel), Phase 3 (feishu chat Channel), Phase 4 (Bundle infra + first bundle feishu-cc + e2e 27/29), Phase 5 (`/session:new template=` + e2e 25/26), Phase 6 (agents.yaml dissolution → plugin manifest `agent_kinds:` block), Phase 7 (multi-session-per-instance + per-file agent split + e2e 28), Phase 8 closeout (concepts.md rev-11, `mix esr.gen_bundle_docs` + `mix esr.check_bundles` CI gate, e2e 30 stub_agent abstraction-validation). Closes the "Channel abstraction" pending row + dissolves `agents.yaml` end-to-end. Bundle is now first-class runtime tier alongside Plugin / Agent / Session.
