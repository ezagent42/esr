# ESR CLI reference

_Auto-generated by `scripts/gen_cli_reference.py` on 2026-04-29._
_Don't hand-edit — modify the click docstrings in `py/src/esr/cli/`
and re-run `bash scripts/gen-docs.sh`._

---

## `cli`

> ``esr`` — command-line entry to a running esrd.

```
Usage: cli [OPTIONS] COMMAND [ARGS]...

  ``esr`` — command-line entry to a running esrd.

Options:
  --instance TEXT   Runtime instance name (default: 'default').
  --esrd-home TEXT  Override ESRD_HOME root (default: ~/.esrd).
  --help            Show this message and exit.

Commands:
  actors      Query the live actor registry (PRD 07 F15).
  adapter     Adapter install / instance / list operations.
  adapters    Plural alias — list configured adapter instances (from...
  admin       Administrative commands (queue-based).
  cap         Manage capabilities (who holds which permission).
  cmd         Command (pattern) install / compile / run / stop operations.
  daemon      Manage the esrd background daemon (start / stop / restart /...
  deadletter  DeadLetter queue inspection + recovery (PRD 07 F19).
  debug       Admin / debug commands (PRD 07 F18).
  drain       Gracefully stop every live topology (PRD 07 F21).
  handler     Handler install / list / remove operations.
  lint        Run the import allow-list purity scan over a directory tree...
  notify      Send a notification via the ESR 开发助手 bot.
  reload      Reload the current esrd via launchctl kickstart.
  scenario    E2E scenario orchestration (PRD 07 F20).
  status      Probe runtime reachability (PRD 07 F02).
  telemetry   Live telemetry subscriptions (PRD 07 F17).
  trace       Read the runtime telemetry-buffer ring (PRD 07 F16).
  use         Set or print the target esrd endpoint (PRD 07 F01).
  user        Manage esr users (the principal identity behind capabilities).
  workspace   Workspace (CC session template) management (PRD v0.2 §3.4).
```

### `cli actors`

> Query the live actor registry (PRD 07 F15).

```
Usage: cli actors [OPTIONS] COMMAND [ARGS]...

  Query the live actor registry (PRD 07 F15).

Options:
  --help  Show this message and exit.

Commands:
  inspect  Dump one actor's state + metadata.
  list     Enumerate every live actor as ``<actor_id> pid=<0.N.M>``.
  logs     Recent telemetry / events for this actor.
  tree     Render the depends_on hierarchy as an indented tree.
```

#### `cli actors inspect`

> Dump one actor's state + metadata.

```
Usage: cli actors inspect [OPTIONS] ACTOR_ID

  Dump one actor's state + metadata.

Options:
  --field TEXT  Dotted path into the describe map (e.g. state.session_name).
                When set, echoes only the value at the path — scripts can
                consume stdout directly without JSON parsing.
  --help        Show this message and exit.
```

#### `cli actors list`

> Enumerate every live actor as ``<actor_id>  pid=<0.N.M>``.

```
Usage: cli actors list [OPTIONS]

  Enumerate every live actor as ``<actor_id>  pid=<0.N.M>``.

  The BEAM pid encodes the live supervisor slot (sig-A) — printing it verbatim
  lets operators correlate ``esr actors list`` rows with ``observer_cli`` /
  ``:observer`` / runtime telemetry without a second tool call.
  ``#PID<0.N.M>`` is stripped to ``<0.N.M>`` for regex friendliness (scenario
  sig-A expects ``pid=<0\.\d+\.\d+>``).

Options:
  --help  Show this message and exit.
```

#### `cli actors logs`

> Recent telemetry / events for this actor.

```
Usage: cli actors logs [OPTIONS] ACTOR_ID

  Recent telemetry / events for this actor.

Options:
  -f, --follow  Stream new lines (Phase 8 only).
  --help        Show this message and exit.
```

#### `cli actors tree`

> Render the depends_on hierarchy as an indented tree.

```
Usage: cli actors tree [OPTIONS]

  Render the depends_on hierarchy as an indented tree.

Options:
  --help  Show this message and exit.
```

### `cli adapter`

> Adapter install / instance / list operations.

```
Usage: cli adapter [OPTIONS] COMMAND [ARGS]...

  Adapter install / instance / list operations.

Options:
  --help  Show this message and exit.

Commands:
  add      Register a new adapter instance (PRD 07 F04).
  feishu   Feishu adapter commands.
  install  Validate a local adapter package (PRD 07 F03).
  list     List installed adapter types (PRD 07 F05).
  remove   Stop and unregister an adapter instance (PR-L 2026-04-28).
  rename   Rename an adapter instance (PR-O 2026-04-28).
```

#### `cli adapter add`

> Register a new adapter instance (PRD 07 F04).

```
Usage: cli adapter add [OPTIONS] INSTANCE_NAME [CONFIG_ARGS]...

  Register a new adapter instance (PRD 07 F04).

  Flag pairs (``--key value`` or ``--key=value``) are collected into the
  instance's AdapterConfig. Writes to ``~/.esrd/<instance>/adapters.yaml`` —
  the "esrd instance" here is hardcoded to ``default`` in v0.1 (separated-
  instance support lives with Phase 1 F18 esrd multi-instance config).

Options:
  --type TEXT  Adapter type name.  [required]
  --help       Show this message and exit.
```

#### `cli adapter feishu`

> Feishu adapter commands.

```
Usage: cli adapter feishu [OPTIONS] COMMAND [ARGS]...

  Feishu adapter commands.

Options:
  --help  Show this message and exit.

Commands:
  create-app  Interactive wizard: prefilled URL → creds → validate →...
```

##### `cli adapter feishu create-app`

> Interactive wizard: prefilled URL → creds → validate → queue submit.

```
Usage: cli adapter feishu create-app [OPTIONS]

  Interactive wizard: prefilled URL → creds → validate → queue submit.

  The flow is strictly linear — no retries, no interactive corrections. If
  validation fails, re-run the command; this keeps the wizard tiny enough to
  audit at a glance and makes the failure mode trivial to test (single call to
  ``_validate_creds``).

Options:
  --name TEXT              Human-readable app name shown in Feishu backend.
                           [required]
  --target-env [prod|dev]  Which ESR environment receives the register_adapter
                           command (prod → ~/.esrd, dev → ~/.esrd-dev).
                           [required]
  --wait / --no-wait       Wait for the admin Dispatcher to complete the
                           registration (default) or fire-and-forget.
  --timeout INTEGER        With --wait, seconds to poll admin_queue/completed
                           before giving up.
  --help                   Show this message and exit.
```

#### `cli adapter install`

> Validate a local adapter package (PRD 07 F03).

```
Usage: cli adapter install [OPTIONS] SOURCE

  Validate a local adapter package (PRD 07 F03).

  v0.1 scope: parse ``esr.toml``, verify the source package exists, and run
  ``esr.verify.io_permission.scan_adapter`` against the adapter module. Full
  fetch-and-register into ``~/.esrd/<instance>/adapters.yaml`` lands with
  runtime wiring (Phase 8). This command is offline.

Options:
  --help  Show this message and exit.
```

#### `cli adapter list`

> List installed adapter types (PRD 07 F05).

```
Usage: cli adapter list [OPTIONS]

  List installed adapter types (PRD 07 F05).

  Scans ``adapters/<name>/esr.toml`` under the current working directory —
  this is a read-only offline operation (PRD 07 F23).

Options:
  --help  Show this message and exit.
```

#### `cli adapter remove`

> Stop and unregister an adapter instance (PR-L 2026-04-28).

```
Usage: cli adapter remove [OPTIONS] INSTANCE_NAME

  Stop and unregister an adapter instance (PR-L 2026-04-28).

  Calls esrd's ``cli:adapters/remove`` Phoenix-channel topic which: 1.
  Terminates the Python adapter sidecar (WorkerSupervisor). 2. Terminates the
  Elixir FAA peer (AdminSession). 3. Rewrites adapters.yaml without the
  instance entry.

  Errors out if the instance isn't in adapters.yaml. Use this when you
  registered an adapter into the wrong environment (prod vs dev) or want to
  retire a Feishu app.

Options:
  --help  Show this message and exit.
```

#### `cli adapter rename`

> Rename an adapter instance (PR-O 2026-04-28).

```
Usage: cli adapter rename [OPTIONS] OLD_INSTANCE_NAME NEW_INSTANCE_NAME

  Rename an adapter instance (PR-O 2026-04-28).

  Equivalent to `adapter remove <old> && adapter add <new>` with the same
  config preserved. Terminates the old FAA peer + Python sidecar, rewrites
  adapters.yaml with the new key, then re-spawns under the new name. Validates
  the new name with the same ASCII rules as `adapter add` (PR-M).

  Use case: registered an adapter with a non-ASCII or temporary name and want
  to migrate without re-entering app_id/secret.

Options:
  --help  Show this message and exit.
```

### `cli adapters`

> Plural alias — list configured adapter instances (from adapters.yaml).

```
Usage: cli adapters [OPTIONS] COMMAND [ARGS]...

  Plural alias — list configured adapter instances (from adapters.yaml).

Options:
  --help  Show this message and exit.

Commands:
  list  List configured adapter instances from...
```

#### `cli adapters list`

> List configured adapter instances from ~/.esrd/default/adapters.yaml.

```
Usage: cli adapters list [OPTIONS]

  List configured adapter instances from ~/.esrd/default/adapters.yaml.

Options:
  --help  Show this message and exit.
```

### `cli admin`

> Administrative commands (queue-based).

```
Usage: cli admin [OPTIONS] COMMAND [ARGS]...

  Administrative commands (queue-based).

Options:
  --help  Show this message and exit.

Commands:
  submit  Submit an admin command to the queue.
```

#### `cli admin submit`

> Submit an admin command to the queue.

```
Usage: cli admin submit [OPTIONS] KIND

  Submit an admin command to the queue.

  Writes ``<admin_queue>/pending/<ULID>.yaml`` atomically (``.tmp`` + rename).
  With ``--wait``, polls ``completed/`` and ``failed/`` for the dispatcher's
  result and exits 0/1 accordingly; ``--timeout`` bounds the wait and exits 3
  if exceeded.

Options:
  --arg TEXT          K=V argument pair; repeatable.
  --wait / --no-wait
  --timeout INTEGER   Wait timeout in seconds.
  --help              Show this message and exit.
```

### `cli cap`

> Manage capabilities (who holds which permission).

```
Usage: cli cap [OPTIONS] COMMAND [ARGS]...

  Manage capabilities (who holds which permission).

Options:
  --help  Show this message and exit.

Commands:
  grant    Grant ``permission`` to ``principal_id``.
  list     Show every registered permission grouped by declaring module.
  revoke   Revoke ``permission`` from ``principal_id``.
  show     Pretty-print one principal's entry from ``capabilities.yaml``.
  who-can  Reverse lookup — list principals whose held capabilities grant...
```

#### `cli cap grant`

> Grant ``permission`` to ``principal_id``.

```
Usage: cli cap grant [OPTIONS] PRINCIPAL_ID PERMISSION

  Grant ``permission`` to ``principal_id``.

  Creates the principal entry if missing; idempotent if the permission is
  already held. Preserves header comments and formatting in
  ``capabilities.yaml`` via ruamel round-trip.

  PR-18 2026-04-28: aliases recognised — ``admin`` rewrites to ``*``
  (wildcard, full grant) before persistence. Operators see the canonical
  string in ``capabilities.yaml`` so the wire format stays uniform with what
  the Lane A checker matches against.

Options:
  --kind TEXT  Principal kind (default: feishu_user)
  --note TEXT  Human-readable note for new principals
  --help       Show this message and exit.
```

#### `cli cap list`

> Show every registered permission grouped by declaring module.

```
Usage: cli cap list [OPTIONS]

  Show every registered permission grouped by declaring module.

  Reads the JSON snapshot esrd writes at boot. If esrd has never started under
  the current `ESRD_HOME`, the snapshot is absent and this command exits 1.

Options:
  --help  Show this message and exit.
```

#### `cli cap revoke`

> Revoke ``permission`` from ``principal_id``.

```
Usage: cli cap revoke [OPTIONS] PRINCIPAL_ID PERMISSION

  Revoke ``permission`` from ``principal_id``.

  No-op with a message if the principal or permission isn't found. The
  principal entry is retained even if its capabilities list becomes empty —
  note/kind persist for future grants.

  PR-18 2026-04-28: ``admin`` alias resolves to ``*`` here too so grant/revoke
  pairs are symmetric.

Options:
  --help  Show this message and exit.
```

#### `cli cap show`

> Pretty-print one principal's entry from ``capabilities.yaml``.

```
Usage: cli cap show [OPTIONS] PRINCIPAL_ID

  Pretty-print one principal's entry from ``capabilities.yaml``.

Options:
  --help  Show this message and exit.
```

#### `cli cap who-can`

> Reverse lookup — list principals whose held capabilities grant ``permission``.

```
Usage: cli cap who-can [OPTIONS] PERMISSION

  Reverse lookup — list principals whose held capabilities grant
  ``permission``.

  Uses the same wildcard rules as `CapabilitiesChecker._matches` (runtime Lane
  A) so the CLI answer can't drift from the live enforcement. An empty match
  set is NOT an error — prints a note on stderr, exits 0.

Options:
  --help  Show this message and exit.
```

### `cli cmd`

> Command (pattern) install / compile / run / stop operations.

```
Usage: cli cmd [OPTIONS] COMMAND [ARGS]...

  Command (pattern) install / compile / run / stop operations.

Options:
  --help  Show this message and exit.

Commands:
  compile  Compile a pattern .py file to canonical YAML (PRD 07 F10).
  install  Install a pattern: resolve deps + compile to YAML (PRD 07 F08).
  list     List available patterns (PRD 07 F09 subset).
  restart  Stop and immediately re-instantiate a topology (PRD 07 F13).
  run      Instantiate a registered command (PRD 07 F11).
  show     Pretty-print a compiled command's topology (PRD 06 F10).
  stop     Deactivate a live topology instantiation (PRD 07 F12).
```

#### `cli cmd compile`

> Compile a pattern .py file to canonical YAML (PRD 07 F10).

```
Usage: cli cmd compile [OPTIONS] NAME

  Compile a pattern .py file to canonical YAML (PRD 07 F10).

Options:
  -o, --output FILE  Where to write the compiled YAML (default:
                     patterns/.compiled/<name>.yaml)
  --help             Show this message and exit.
```

#### `cli cmd install`

> Install a pattern: resolve deps + compile to YAML (PRD 07 F08).

```
Usage: cli cmd install [OPTIONS] SOURCE

  Install a pattern: resolve deps + compile to YAML (PRD 07 F08).

  Checks that every adapter type and handler referenced by the pattern's nodes
  has an installed manifest under ``adapters/`` / ``handlers/`` (PRD 06 F08).
  Missing deps → list + exit nonzero. On success, writes ``<compiled-
  dir>/<name>.yaml`` (PRD 06 F09).

Options:
  --compiled-dir DIRECTORY  Directory for compiled YAML (default:
                            patterns/.compiled/).
  --help                    Show this message and exit.
```

#### `cli cmd list`

> List available patterns (PRD 07 F09 subset).

```
Usage: cli cmd list [OPTIONS]

  List available patterns (PRD 07 F09 subset).

  Reads ``patterns/*.py`` files; each file's name (minus ``.py``) is the
  command name. Fully offline.

Options:
  --help  Show this message and exit.
```

#### `cli cmd restart`

> Stop and immediately re-instantiate a topology (PRD 07 F13).

```
Usage: cli cmd restart [OPTIONS] NAME

  Stop and immediately re-instantiate a topology (PRD 07 F13).

  State preservation is a spec §6.5 guarantee: PeerServer state persisted via
  F18 ETS survives the stop/run boundary, so a restart keeps handler counters
  / dedup sets intact.

Options:
  --param TEXT         Param binding (``key=value``); identifies the target
                       and primes the restart.
  --compiled-dir PATH  Override the .compiled/ lookup path.
  --help               Show this message and exit.
```

#### `cli cmd run`

> Instantiate a registered command (PRD 07 F11).

```
Usage: cli cmd run [OPTIONS] NAME

  Instantiate a registered command (PRD 07 F11).

  Loads the compiled artifact from ``.compiled/<name>.yaml``, validates
  declared params are provided, and submits to the runtime. Prints the
  instantiation handle on stdout. Timeout 30 s per PRD.

Options:
  --param TEXT         Param binding (``key=value``); repeat for multiple
                       params.
  --compiled-dir PATH  Override the .compiled/ lookup path (default:
                       ~/.esrd/default/commands/.compiled).
  --help               Show this message and exit.
```

#### `cli cmd show`

> Pretty-print a compiled command's topology (PRD 06 F10).

```
Usage: cli cmd show [OPTIONS] NAME

  Pretty-print a compiled command's topology (PRD 06 F10).

Options:
  --compiled-dir DIRECTORY  Directory holding compiled YAMLs (default:
                            patterns/.compiled/).
  --help                    Show this message and exit.
```

#### `cli cmd stop`

> Deactivate a live topology instantiation (PRD 07 F12).

```
Usage: cli cmd stop [OPTIONS] NAME

  Deactivate a live topology instantiation (PRD 07 F12).

  Identifies the target by ``(name, params)`` — the same key Registry.register
  used — and triggers Registry.deactivate on the runtime, cascading tear-down
  in reverse depends_on order per spec §6.5.

Options:
  --param TEXT  Param binding (``key=value``); must match the live (name,
                params) key.
  --help        Show this message and exit.
```

### `cli daemon`

> Manage the esrd background daemon (start / stop / restart / status).

```
Usage: cli daemon [OPTIONS] COMMAND [ARGS]...

  Manage the esrd background daemon (start / stop / restart / status).

Options:
  --help  Show this message and exit.

Commands:
  restart  Restart esrd via launchctl kickstart -k.
  start    Bring up esrd via launchctl bootstrap.
  status   Show whether esrd is running, with pid + label.
  stop     Stop esrd via launchctl bootout.
```

#### `cli daemon restart`

> Restart esrd via launchctl kickstart -k.

```
Usage: cli daemon restart [OPTIONS]

  Restart esrd via launchctl kickstart -k.

  `kickstart -k` sends SIGTERM to the running process and waits for launchd to
  respawn it. Faster than stop+start (no plist re-load), and KeepAlive=true on
  our plist guarantees the respawn.

Options:
  --help  Show this message and exit.
```

#### `cli daemon start`

> Bring up esrd via launchctl bootstrap.

```
Usage: cli daemon start [OPTIONS]

  Bring up esrd via launchctl bootstrap.

  Idempotent — if already loaded + running, it's a no-op (launchctl returns
  "service already loaded" which we report cleanly). If loaded but stopped,
  this sends ``kickstart``.

Options:
  --help  Show this message and exit.
```

#### `cli daemon status`

> Show whether esrd is running, with pid + label.

```
Usage: cli daemon status [OPTIONS]

  Show whether esrd is running, with pid + label.

  Reads ``launchctl list <label>`` for the active state. Exit 0 when running,
  1 when stopped.

Options:
  --help  Show this message and exit.
```

#### `cli daemon stop`

> Stop esrd via launchctl bootout.

```
Usage: cli daemon stop [OPTIONS]

  Stop esrd via launchctl bootout.

  Bootout signals the daemon process and removes the plist from the launchd
  registry. Use ``esr daemon start`` to bring it back.

Options:
  --help  Show this message and exit.
```

### `cli deadletter`

> DeadLetter queue inspection + recovery (PRD 07 F19).

```
Usage: cli deadletter [OPTIONS] COMMAND [ARGS]...

  DeadLetter queue inspection + recovery (PRD 07 F19).

Options:
  --help  Show this message and exit.

Commands:
  flush  Empty the dead-letter queue (admin op — logs a warning).
  list   List every entry currently in the runtime's dead-letter queue.
  retry  Re-enqueue a dead-letter entry for another delivery attempt.
```

#### `cli deadletter flush`

> Empty the dead-letter queue (admin op — logs a warning).

```
Usage: cli deadletter flush [OPTIONS]

  Empty the dead-letter queue (admin op — logs a warning).

Options:
  --help  Show this message and exit.
```

#### `cli deadletter list`

> List every entry currently in the runtime's dead-letter queue.

```
Usage: cli deadletter list [OPTIONS]

  List every entry currently in the runtime's dead-letter queue.

Options:
  --help  Show this message and exit.
```

#### `cli deadletter retry`

> Re-enqueue a dead-letter entry for another delivery attempt.

```
Usage: cli deadletter retry [OPTIONS] ENTRY_ID

  Re-enqueue a dead-letter entry for another delivery attempt.

Options:
  --help  Show this message and exit.
```

### `cli debug`

> Admin / debug commands (PRD 07 F18).

```
Usage: cli debug [OPTIONS] COMMAND [ARGS]...

  Admin / debug commands (PRD 07 F18).

Options:
  --help  Show this message and exit.

Commands:
  inject  Inject a synthetic event straight into a peer's mailbox.
  pause   Pause a peer (inbound events queue until resume).
  replay  Re-process a previously-received event by its envelope id.
  resume  Resume a paused peer, draining queued events FIFO.
```

#### `cli debug inject`

> Inject a synthetic event straight into a peer's mailbox.

```
Usage: cli debug inject [OPTIONS]

  Inject a synthetic event straight into a peer's mailbox.

Options:
  --to TEXT     Target actor_id.  [required]
  --event TEXT  Event JSON body.  [required]
  --help        Show this message and exit.
```

#### `cli debug pause`

> Pause a peer (inbound events queue until resume).

```
Usage: cli debug pause [OPTIONS] ACTOR_ID

  Pause a peer (inbound events queue until resume).

Options:
  --help  Show this message and exit.
```

#### `cli debug replay`

> Re-process a previously-received event by its envelope id.

```
Usage: cli debug replay [OPTIONS] MSG_ID

  Re-process a previously-received event by its envelope id.

Options:
  --help  Show this message and exit.
```

#### `cli debug resume`

> Resume a paused peer, draining queued events FIFO.

```
Usage: cli debug resume [OPTIONS] ACTOR_ID

  Resume a paused peer, draining queued events FIFO.

Options:
  --help  Show this message and exit.
```

### `cli drain`

> Gracefully stop every live topology (PRD 07 F21).

```
Usage: cli drain [OPTIONS]

  Gracefully stop every live topology (PRD 07 F21).

  Traverses topologies in reverse depends_on order, deactivating each. Blocks
  until either every topology has stopped or the timeout expires. Reports a
  summary on stdout and exits non-zero if any topology failed to stop in time.

Options:
  --timeout TEXT  Max wait per topology (e.g. ``30s``, ``2m``). Default:
                  server-side config.
  --help          Show this message and exit.
```

### `cli handler`

> Handler install / list / remove operations.

```
Usage: cli handler [OPTIONS] COMMAND [ARGS]...

  Handler install / list / remove operations.

Options:
  --help  Show this message and exit.

Commands:
  install  Validate a local handler package (PRD 07 F06).
  list     List installed handlers (PRD 07 F07).
```

#### `cli handler install`

> Validate a local handler package (PRD 07 F06).

```
Usage: cli handler install [OPTIONS] SOURCE

  Validate a local handler package (PRD 07 F06).

  v0.1 scope: parse ``esr.toml``, run ``scan_imports`` on every ``.py`` file
  under ``src/``. Full fetch-and-register into
  ``~/.esrd/<instance>/handlers.yaml`` lands with runtime wiring (Phase 8).
  This command is offline.

Options:
  --help  Show this message and exit.
```

#### `cli handler list`

> List installed handlers (PRD 07 F07).

```
Usage: cli handler list [OPTIONS]

  List installed handlers (PRD 07 F07).

Options:
  --help  Show this message and exit.
```

### `cli lint`

> Run the import allow-list purity scan over a directory tree (PRD 07 F14).

```
Usage: cli lint [OPTIONS] PATH

  Run the import allow-list purity scan over a directory tree (PRD 07 F14).

  Walks every ``.py`` file under ``path`` and applies
  ``esr.verify.purity.scan_imports``. Prints each violation as
  ``<file>:<lineno>: <message>`` and exits nonzero if any violation surfaced.

Options:
  --help  Show this message and exit.
```

### `cli notify`

> Send a notification via the ESR 开发助手 bot.

```
Usage: cli notify [OPTIONS]

  Send a notification via the ESR 开发助手 bot.

  Writes a ``kind=notify`` admin command to the queue; the
  ``Esr.Admin.Commands.Notify`` handler on the dispatcher side composes the DM
  and submits it through the existing Feishu adapter (spec §7.7). Fire-and-
  forget by default — see module docstring for why ``--wait`` is not exposed
  here.

Options:
  --type [breaking|info|reload-complete]
                                  Notification kind: ``breaking`` (post-merge
                                  hook), ``info`` (generic operator message),
                                  ``reload-complete`` (emitted by the post-
                                  boot dispatcher after launchctl kickstart).
                                  [required]
  --since TEXT                    Git SHA the commit range starts from (pre-
                                  merge HEAD for ``breaking``; last_reload SHA
                                  for ``reload-complete``).
  --details TEXT                  Free-text body appended to the DM; the post-
                                  merge hook passes the raw ``git log`` output
                                  here (multi-line ok).
  --to TEXT                       Target Feishu open_id; omit to fan out to
                                  all admins listed in the runtime admin-
                                  principal config.
  --help                          Show this message and exit.
```

### `cli reload`

> Reload the current esrd via launchctl kickstart.

```
Usage: cli reload [OPTIONS]

  Reload the current esrd via launchctl kickstart.

  Run ``esr reload`` (or ``esr reload --acknowledge-breaking``) after merging
  changes into ``main`` so the always-on prod / dev esrd picks up new
  patterns, handlers, or config. The admin dispatcher scans ``git log
  <last_reload>..HEAD`` for breaking-change markers and refuses to kickstart
  until the operator acknowledges them.

Options:
  --acknowledge-breaking  Confirm awareness of breaking commits since last
                          reload (required when post-merge hook flagged ``!:``
                          or ``BREAKING CHANGE:`` footer in the merge range).
  --dry-run               Skip the launchctl kickstart; the dispatcher records
                          the scan result but does not actually reload.
  --wait / --no-wait      Block until the dispatcher writes the completed /
                          failed counterpart (default: --wait). Pass --no-wait
                          for fire-and-forget submission (e.g. from CI /
                          hooks).
  --timeout INTEGER       Wait timeout in seconds (ignored when --no-wait).
                          [default: 60]
  --help                  Show this message and exit.
```

### `cli scenario`

> E2E scenario orchestration (PRD 07 F20).

```
Usage: cli scenario [OPTIONS] COMMAND [ARGS]...

  E2E scenario orchestration (PRD 07 F20).

Options:
  --help  Show this message and exit.

Commands:
  run  Run an E2E scenario from ``scenarios/<name>.yaml`` (spec v2.1 §4.2).
```

#### `cli scenario run`

> Run an E2E scenario from ``scenarios/<name>.yaml`` (spec v2.1 §4.2).

```
Usage: cli scenario run [OPTIONS] NAME

  Run an E2E scenario from ``scenarios/<name>.yaml`` (spec v2.1 §4.2).

  Each step under ``steps:`` is executed: 1. Run ``command`` as a subprocess
  (shell=True) with    ``timeout_sec`` deadline. 2. Compare exit status to
  ``expect_exit`` (default 0). 3. Match stdout against ``expect_stdout_match``
  regex. On any miss, report the step failed and continue. Exit 0 iff every
  step passed; exit 1 otherwise.

Options:
  -v, --verbose  Per-step output.
  --help         Show this message and exit.
```

### `cli status`

> Probe runtime reachability (PRD 07 F02).

```
Usage: cli status [OPTIONS]

  Probe runtime reachability (PRD 07 F02).

  Connects a short-lived ChannelClient to the current context's adapter_hub
  socket. Prints ``<endpoint> — OK`` on success, or ``<endpoint> — UNREACHABLE
  (<reason>)`` with a non-zero exit code so shells / scripts can detect it.

Options:
  --help  Show this message and exit.
```

### `cli telemetry`

> Live telemetry subscriptions (PRD 07 F17).

```
Usage: cli telemetry [OPTIONS] COMMAND [ARGS]...

  Live telemetry subscriptions (PRD 07 F17).

Options:
  --help  Show this message and exit.

Commands:
  subscribe  Stream events matching ``<pattern>`` as they fire (PRD 07 F17).
```

#### `cli telemetry subscribe`

> Stream events matching ``<pattern>`` as they fire (PRD 07 F17).

```
Usage: cli telemetry subscribe [OPTIONS] PATTERN

  Stream events matching ``<pattern>`` as they fire (PRD 07 F17).

  Long-running — terminates on Ctrl-C. Pattern is the Elixir dotted-event
  shape (e.g. ``esr.handler.*``).

Options:
  --format [json|table]  Output format: table (one line per event) or json
                         (ndjson).
  --help                 Show this message and exit.
```

### `cli trace`

> Read the runtime telemetry-buffer ring (PRD 07 F16).

```
Usage: cli trace [OPTIONS]

  Read the runtime telemetry-buffer ring (PRD 07 F16).

  All options narrow the server-side query; the CLI prints the returned events
  one-per-line as ``<ts> <event>  actor_id=<id>`` with remaining kwargs
  echoed.

Options:
  --session TEXT  Scope to one actor_id / session.
  --last TEXT     Time window (e.g. ``5m``, ``1h``).
  --filter TEXT   Event-name regex filter.
  --help          Show this message and exit.
```

### `cli use`

> Set or print the target esrd endpoint (PRD 07 F01).

```
Usage: cli use [OPTIONS] [HOST_PORT]

  Set or print the target esrd endpoint (PRD 07 F01).

  With a ``host:port`` argument, persists ``~/.esr/context`` so future
  commands target that endpoint. Without arguments, prints the current
  endpoint (env ``ESR_CONTEXT`` overrides the file).

Options:
  --help  Show this message and exit.
```

### `cli user`

> Manage esr users (the principal identity behind capabilities).

```
Usage: cli user [OPTIONS] COMMAND [ARGS]...

  Manage esr users (the principal identity behind capabilities).

Options:
  --help  Show this message and exit.

Commands:
  add            Register a new esr user with no feishu binding yet.
  bind-feishu    Bind a Feishu open_id to an existing esr user.
  list           Print every registered esr user, one per line, with...
  remove         Remove an esr user and all their feishu bindings.
  unbind-feishu  Remove a Feishu open_id binding from an esr user.
```

#### `cli user add`

> Register a new esr user with no feishu binding yet.

```
Usage: cli user add [OPTIONS] NAME

  Register a new esr user with no feishu binding yet.

  Bind feishu ids afterwards via ``esr user bind-feishu <name> <feishu_id>``.

Options:
  --help  Show this message and exit.
```

#### `cli user bind-feishu`

> Bind a Feishu open_id to an existing esr user.

```
Usage: cli user bind-feishu [OPTIONS] NAME FEISHU_USER_ID

  Bind a Feishu open_id to an existing esr user.

  Multiple feishu ids per user are allowed (one per registered Feishu app —
  open_ids are app-scoped). Idempotent: re-binding the same pair is a no-op.

Options:
  --help  Show this message and exit.
```

#### `cli user list`

> Print every registered esr user, one per line, with bound feishu ids.

```
Usage: cli user list [OPTIONS]

  Print every registered esr user, one per line, with bound feishu ids.

Options:
  --help  Show this message and exit.
```

#### `cli user remove`

> Remove an esr user and all their feishu bindings.

```
Usage: cli user remove [OPTIONS] NAME

  Remove an esr user and all their feishu bindings.

  Does NOT cascade-delete capabilities granted to the user (run ``esr cap
  revoke`` separately if you want a clean wipe).

Options:
  --help  Show this message and exit.
```

#### `cli user unbind-feishu`

> Remove a Feishu open_id binding from an esr user.

```
Usage: cli user unbind-feishu [OPTIONS] NAME FEISHU_USER_ID

  Remove a Feishu open_id binding from an esr user.

  PR-21s (2026-04-29): also auto-revokes the 4 bootstrap caps that bind-feishu
  auto-granted. Symmetric — bind grants, unbind revokes. Other manually-added
  caps on the open_id are preserved.

Options:
  --help  Show this message and exit.
```

### `cli workspace`

> Workspace (CC session template) management (PRD v0.2 §3.4).

```
Usage: cli workspace [OPTIONS] COMMAND [ARGS]...

  Workspace (CC session template) management (PRD v0.2 §3.4).

Options:
  --help  Show this message and exit.

Commands:
  add     Declare a workspace template for `/new-session <name>`.
  list    List declared workspaces.
  remove  Remove a workspace declaration.
```

#### `cli workspace add`

> Declare a workspace template for `/new-session <name>`.

```
Usage: cli workspace add [OPTIONS] NAME

  Declare a workspace template for `/new-session <name>`.

  PR-22 (2026-04-29): `--root` removed. Workspace no longer carries a repo
  identity — repo path is per-session (`root=` arg in `/new-session` slash
  command).

Options:
  --owner TEXT      esr username this workspace belongs to (PR-21c).
                    [required]
  --start-cmd TEXT  [required]
  --role TEXT
  --chat TEXT       <chat_id>:<app_id>:<kind> triple; repeat for multiple
                    chats
  --env TEXT        KEY=VAL; repeat for multiple env vars
  --help            Show this message and exit.
```

#### `cli workspace list`

> List declared workspaces.

```
Usage: cli workspace list [OPTIONS]

  List declared workspaces.

Options:
  --help  Show this message and exit.
```

#### `cli workspace remove`

> Remove a workspace declaration.

```
Usage: cli workspace remove [OPTIONS] NAME

  Remove a workspace declaration.

Options:
  --help  Show this message and exit.
```

